---
phase: 01-memory-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .opencode/.env (if auto-start triggers need env vars)
  - skills/echoes-append-to-daily-log/SKILL.md (extend for auto-logging)
  - echoes-state.json (session tracking)
autonomous: true
requirements: [MEM-01, MEM-02]
user_setup: []

must_haves:
  truths:
    - Every assistant response auto-saves context to the daily log
    - Session start loads prior session summary without manual command
    - User can see a summary of last session's decisions
  artifacts:
    - echoes-state.json (auto-managed)
    - EchoesVault/daily/ (auto-appended entries)
  key_links:
    - echoes-state.json session.started/saved flags tied to session lifecycle
    - Daily log entries timestamped and traceable to interactions
---

<objective>
Automate the EchoesVault session lifecycle: auto-save context on every interaction (making disconnects safe) and auto-load a summary on session start.

Purpose: Eliminate manual memory commands — session start/end/save happen automatically. A disconnect mid-task loses nothing.
Output: Modified EchoesVault skills + session automation
</objective>

<execution_context>
@/home/khuchinque/.opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/khuchinque/.opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-reliability/01-CONTEXT.md
@echoes-state.json
@memory-protocol.md
@skills/echoes-append-to-daily-log/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Extend echoes-append-to-daily-log for auto-logging</name>
  <files>skills/echoes-append-to-daily-log/SKILL.md</files>
  <read_first>skills/echoes-append-to-daily-log/SKILL.md</read_first>
  <action>Update the SKILL.md to add an auto-trigger mode alongside the existing manual triggers. The current skill only fires on explicit triggers (task completion, context switch, explicit user command). Add a new rule: "Auto-log: After EVERY assistant response that produced meaningful output (code, decisions, file edits), append a concise one-line summary to the daily log." Keep existing trigger conditions — just add the auto-log rule at the top with lower priority (auto-log can be skipped if log is identical to last entry). Prefix auto-log entries with a marker to distinguish from manual ones.</action>
  <verify>grep -q "auto-log" skills/echoes-append-to-daily-log/SKILL.md</verify>
  <acceptance_criteria>
    - skills/echoes-append-to-daily-log/SKILL.md contains auto-log trigger condition
    - Manual trigger conditions are preserved
    - Auto-log entries are distinguishable from manual entries
  </acceptance_criteria>
  <done>SKILL.md has auto-log rule, manual triggers preserved</done>
</task>

<task type="auto">
  <name>task 2: Create session-summary skill for context loading</name>
  <files>skills/echoes-load-session-context/SKILL.md</files>
  <read_first>echoes-state.json, EchoesVault/daily/2026-07-25.md</read_first>
  <action>Create a new skill at `skills/echoes-load-session-context/SKILL.md`. This skill defines the session start behavior per user decisions (D-03, D-04): On session start, read the most recent daily log entry, extract key decisions and last state, and present a concise summary to the user. Then offer: "Want the full context from last session?" — if user says yes, load the full daily log. The skill should read `echoes-state.json` to find the last session timestamp and the last daily log entry. Format: YAML frontmatter matching other EchoesVault skills, with clear trigger conditions and payload.</action>
  <verify>test -f skills/echoes-load-session-context/SKILL.md</verify>
  <acceptance_criteria>
    - File exists at skills/echoes-load-session-context/SKILL.md
    - Defines session-start trigger condition
    - Specifies summary format (key decisions, last state + offer for full context)
  </acceptance_criteria>
  <done>SKILL.md created with session-loading protocol</done>
</task>

<task type="auto">
  <name>task 3: Update echoes-state.json to track auto-save state</name>
  <files>echoes-state.json</files>
  <read_first>echoes-state.json</read_first>
  <action>Update echoes-state.json to support auto-save lifecycle. The current state has `session.saved: false` and `session.lastSave: null`. Ensure the state file is structured so automated tools can update it: add an `autoSave` field with `enabled: true` and `lastAutoSave: null` (timestamp). Keep backward compatibility with all existing fields. The file is read/written by tools so don't break existing structure.</action>
  <verify>node -e "const s=require('./echoes-state.json'); console.log(s.autoSave?.enabled===true)" 2>&1 || echo "FAIL: checks echoes-state.json has autoSave field"</verify>
  <acceptance_criteria>
    - echoes-state.json has autoSave.enabled: true
    - All original fields preserved (version, initialized, session, stats)
    - JSON valid
  </acceptance_criteria>
  <done>echoes-state.json extended with auto-save tracking</done>
</task>

</tasks>

---
phase: 01-memory-reliability
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - skills/echoes-search-vault-pages/SKILL.md (extend for 4-layer search)
autonomous: true
requirements: [MEM-03]
user_setup: []

must_haves:
  truths:
    - User can query memory via /memory command
    - Agent searches memory autonomously during chat when relevant
    - Search spans all 4 cognitive layers (Working/Episodic/Semantic/Procedural)
  artifacts:
    - Command or handler for /memory
    - Extended search skill covering all layers
  key_links:
    - memory-list.md (tool reference for search)
    - 4-layer protocol linked to search results
---

<objective>
Implement unified memory search across all 4 cognitive layers — accessible via explicit `/memory "query"` command and implicit natural language chat search.

Purpose: User can retrieve past context from any session, on demand or via autonomous agent search.
Output: Extended search skill + /memory command handler
</objective>

<execution_context>
@/home/khuchinque/.opencode/.opencode/get-shit-done/workflows/execute-plan.md
@/home/khuchinque/.opencode/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-memory-reliability/01-CONTEXT.md
@memory-list.md
@memory-protocol.md
@skills/echoes-search-vault-pages/SKILL.md
@EchoesVault/index.md
</context>

<tasks>

<task type="auto">
  <name>task 1: Extend echoes-search-vault-pages for 4-layer search</name>
  <files>skills/echoes-search-vault-pages/SKILL.md</files>
  <read_first>skills/echoes-search-vault-pages/SKILL.md, memory-list.md, memory-protocol.md</read_first>
  <action>Extend the existing search skill to cover ALL 4 memory layers, not just EchoesVault pages. The current skill only searches EchoesVault pages. Update it to also search: the daily log directory (EchoesVault/daily/), Mem0 via mem0-mcp MCP tools (vector memory), and the knowledge graph if available (graphify-out/). Add a unified search method that returns results from all layers with source labels. Keep the existing page search working for backward compat. The extended skill must: accept a query string, search all available layers, deduplicate results, and return them with source attribution (EchoesVault page / daily log / Mem0 vector / graph).</action>
  <verify>grep -qi "4.layer\|all layers\|unified\|mem0\|daily\|graph" skills/echoes-search-vault-pages/SKILL.md</verify>
  <acceptance_criteria>
    - SKILL.md includes search instructions for daily logs
    - SKILL.md includes search instructions for Mem0 vector memory
    - SKILL.md includes search instructions for knowledge graph
    - Results include source labels
    - Original page search preserved
  </acceptance_criteria>
  <done>Search skill spans all 4 memory layers</done>
</task>

<task type="auto">
  <name>task 2: Create /memory command definition</name>
  <files>commands/memory.md</files>
  <read_first>ls commands/ for existing command examples (echoes-*.md)</read_first>
  <action>Create a `/memory` slash command at `commands/memory.md`. This command accepts a query string and returns results across all 4 memory layers. Format: follow the same structure as existing `commands/echoes-*.md` commands (YAML frontmatter with name, description plus markdown body explaining behavior). The command body should: 1) Accept a freeform query, 2) Invoke the unified search skill, 3) Display results grouped by layer, 4) Offer to dive deeper into any result. Pattern after echoes-search-vault-pages but broader.</action>
  <verify>test -f commands/memory.md</verify>
  <acceptance_criteria>
    - commands/memory.md exists with valid format
    - References all 4 memory layers
    - Accepts freeform query parameter
  </acceptance_criteria>
  <done>/memory command defined</done>
</task>

<task type="auto">
  <name>task 3: Add autonomous search trigger to session skill</name>
  <files>skills/echoes-load-session-context/SKILL.md</files>
  <read_first>skills/echoes-load-session-context/SKILL.md</read_first>
  <action>Add an autonomous search trigger to the session context skill (created in 01-01). The skill should include a rule: "During conversation, if the user asks a question where answering requires context from prior sessions, autonomously search memory via the 4-layer search skill and present relevant findings." Include examples of when to search (e.g., "What was the auth approach?" → search memory for "auth", "authentication", "login") and when not to (e.g., "What's 2+2?" → don't search).</action>
  <verify>grep -qi "autonomous\|search\|prior session\|memory" skills/echoes-load-session-context/SKILL.md</verify>
  <acceptance_criteria>
    - Session skill includes autonomous search trigger
    - Includes examples of when/not to trigger search
  </acceptance_criteria>
  <done>Autonomous search trigger defined in session skill</done>
</task>

</tasks>
